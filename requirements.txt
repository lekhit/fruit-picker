# Core Scientific & Vision Libraries
numpy>=1.26.0,<2.0.0
opencv-python>=4.6.0.66,<4.9.0
matplotlib>=3.5.0
pillow>=9.0.0
tqdm>=4.64.0

# Deep Learning Frameworks
# Note: For GPU execution, ensure standard PyTorch with CUDA is installed (e.g. torch, torchvision, torchaudio)
torch>=2.0.0
torchvision>=0.15.0

# Object Detection & Tracking
ultralytics>=8.1.0  # Provides YOLOv11-World and standard multi-object trackers like ByteTrack/BoT-SORT
scikit-learn>=1.0.0 # Sometimes used for custom spatial association

# Instructions for installing SAM-3 and DEVA:
# ----------------------------------------------------
# 1. To install SAM 3 / 3.1:
#    pip install git+https://github.com/facebookresearch/sam3.git
#
# 2. To install DEVA:
#    pip install git+https://github.com/HKUST-ICG/DEVA.git
# ----------------------------------------------------
